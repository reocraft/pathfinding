# pathfinding
Program ran inside the terminal using ncurses that searches for a path from point A to point B on a map with obstacles using breadth first search.
How to run program:
1. Set up ncurses on your terminal.
2. Type "clang -o pathfinding pathfinding.c"
3. Type "./pathfinding"
